## User

Add a `formatDate` helper to src/lib/date.ts and a test for it.

## Assistant

I'll add the helper and a test.

⏺ Read(src/lib/date.ts)
⏺ Write(src/lib/date.ts)
⏺ Write(src/lib/date.test.ts)
⏺ Bash(npm test)

```bash
$ npm test
All tests passed (4 passing)
```

Done — `formatDate` is added with a passing test.
