# Provider avatar assets

These small M9R provider marks are local, replaceable image slots for the presence overlay. They are not official vendor logos; the provider name is shown as text, and the overlay falls back to a letter if an image cannot load. Replace a mark only with an asset whose distribution and trademark use are authorized.
